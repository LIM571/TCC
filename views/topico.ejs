<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Novo Tópico</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style> 
        .body {
            background-color: #2e2e2e;
        }
        .sidebar {
            width: 20%;
        }
    </style>
</head>
<body class="body flex p-6"> <!-- Adicionando padding ao body -->
    <div class="sidebar bg-gray-800 p-6 rounded-lg shadow-lg mt-10 ml-6"> <!-- Margem superior e esquerda -->
        <h1 class="text-white text-3xl mb-4">Fórum</h1>
        <div class="flex justify-between mb-4">
            <a href="/forum" class="text-white">Fórum</a>
            <a href="#" id="addTopico" class="text-white underline cursor-pointer">Adicionar Tópico</a>
        </div>

        <div id="formContainer" class="hidden mb-4">
            <form action="/forum/topicos" method="POST" class="bg-red-800 p-6 rounded-lg shadow-md">
                <div class="mb-4">
                    <label for="titulo" class="block text-white mb-2">Título:</label>
                    <input type="text" id="titulo" name="titulo" required class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-500">
                </div>
                <div class="mb-4">
                    <label for="descricao" class="block text-white mb-2">Descrição:</label>
                    <textarea id="descricao" name="descricao" required class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"></textarea>
                </div>
                <button type="submit" class="bg-red-800 text-white px-4 py-2 rounded-md hover:bg-red-900 focus:outline-none focus:ring-2 focus:ring-red-500">
                    Adicionar Tópico
                </button>
            </form>
        </div>

        <ul>
            <% topicos.forEach(topico => { %>
                <li class="bg-gray-800 p-4 rounded-lg shadow-lg my-4">
                    <h2 class="text-white text-xl font-bold"><%= topico.titulo %></h2>
                    <a href="/forum/<%= topico.id %>" class="text-blue-400">Ver Discussão</a>
                </li>
            <% }) %>
        </ul>
    </div>

    <div class="flex-grow p-6">
        <!-- Esta área pode ser usada para outros conteúdos se necessário -->
    </div>

    <script>
        document.getElementById('addTopico').addEventListener('click', function() {
            const formContainer = document.getElementById('formContainer');
            formContainer.classList.toggle('hidden');
        });
    </script>
</body>
</html>
